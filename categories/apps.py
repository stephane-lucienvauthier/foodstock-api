"""This module defines the configuration for the categories app."""
from django.apps import AppConfig


class CategoriesConfig(AppConfig):
    """
    This class defines the configuration for the categories app.

    Attributes:
        name (str): The app name.
    """

    name = 'categories'
